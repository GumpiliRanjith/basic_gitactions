name: artiface

on: push

jobs:
    yaara:
        runs-on: ubuntu-latest
        steps:
            - name: install repo
              uses: actions/checkout@v4

            - name: install npm
              uses: actions/setup-node@v4
              with:
                node-version: 20

            - name: install dependencies
              run: npm run test

