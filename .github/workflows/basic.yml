name: artiface

on: push

jobs:
    yaara:
        runs-on: ubuntu-latest
        steps:
            - name: create file
              run: |
                 echo "welcome" > file.txt
            - name: upload artifact
              uses: actions/upload-artifact@v4
              with:
                name: file.txt
                path: hello
